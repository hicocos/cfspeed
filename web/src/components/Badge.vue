<script setup lang="ts">import {computed} from 'vue';import {label} from '../lib/format';const props=defineProps<{value:string}>();const style=computed(()=>({ok:'success',error:'failed',verified:'success',unselected:'disabled',unchanged:'disabled',preview:'processing',apply:'active',planned:'pending',interrupted:'failed',failed_or_unverified:'failed',interrupted_unverified:'failed'}[props.value]||props.value))</script><template><span :class="['badge',style]">{{label(value)}}</span></template>
