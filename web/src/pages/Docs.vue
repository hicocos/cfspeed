<script setup lang="ts">import Icon from '../components/Icon.vue';import CodeBlock from '../components/CodeBlock.vue';const origin=window.location.origin</script><template><div class="admin-heading"><div><span class="eyebrow">DOCUMENTATION</span><h1>使用文档</h1><p>从优选 IP 到 DNS 同步。</p></div></div><section class="card settings-section"><div class="card-heading"><h2>配置流程</h2></div><ol class="doc-steps"><li><strong>添加 DNS 目标</strong><p>填写 Cloudflare 区域 ID / DNSPod 域名及凭据，保存后保持预览模式。先在提供商控制台准备已有 A 记录。</p></li><li><strong>检查预览计划</strong><p>在总览点击“立即预览”，进入同步记录查看每条解析的“当前 IP → 目标 IP”。预览不会写 DNS。</p></li><li><strong>启用正式同步</strong><p>确认目标正确，停用旧 GitHub 定时任务，再到服务设置显式确认启用。后台开始按周期更新。</p></li></ol></section><section class="card settings-section"><div class="card-heading"><h2>Cloudflare API Token 创建教程</h2></div><ol class="doc-steps"><li><strong>创建令牌</strong><p>在 Cloudflare「我的个人资料 → API 令牌」创建令牌，可用“编辑区域 DNS”模板。</p></li><li><strong>设置权限与资源</strong><p>权限设为「区域 → DNS → 编辑」，区域资源选择「包括 → 特定区域」，仅授权需要同步的域名。</p></li><li><strong>填写并预览</strong><p>将生成的 Token 值填入项目对应 DNS 目标的 API Token 凭据，保存后再点“立即预览”。</p></li></ol></section><section class="card settings-section"><div class="card-heading"><h2>IP 文本接口</h2><p class="muted">返回最近一次成功获取的地址，以逗号分隔。</p></div><CodeBlock :code="`curl ${origin}/ipTop.html`" language="SHELL"/><p class="help">源请求失败时保留旧地址，不拿缓存继续写 DNS。该接口没有测速生成时间，不代表来源一定最新。</p><a href="/ipTop.html" class="text-link" target="_blank" rel="noopener">打开 IP 接口 <Icon name="external" :size="16"/></a></section><section class="card settings-section"><div class="card-heading"><h2>健康检查</h2></div><CodeBlock :code="`curl ${origin}/healthz
curl ${origin}/readyz`" language="SHELL"/><p><code>/healthz</code> 表示进程存活；<code>/readyz</code> 要求本次进程完成成功任务，且不存在未核对写入。预览就绪不等于真实 DNS 写入已验证。</p></section><section class="card settings-section"><div class="card-heading"><h2>同步保护</h2></div><ul class="doc-list"><li>仅更新精确匹配目标的已有 A 记录；不创建、不删除、不启用暂停记录。</li><li>保留 TTL、代理状态和线路设置；IP 不变时跳过。</li><li>写入响应异常后只尝试读回核验，不盲目重发写入。多条操作不是事务。</li><li>Web 管理配置保存在数据卷，重建容器保留；命令行配置与 Web 覆盖的说明见项目 README。</li></ul></section></template>
